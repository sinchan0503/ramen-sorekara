# ラーメン、それから。— Claude Code 運用ガイド

## ブログ概要

- **URL**: ramen-sorekara.com
- **主人公**: ぽん（東京在住OL、20代前半、天然でお転婆）
- **スタック**: Astro / Cloudflare Pages（git pushで自動デプロイ）
- **記事パス**: `src/content/blog/*.md`
- **画像パス**: `public/images/`

---

## 記事執筆ワークフロー

ユーザーがメモ・写真・店名を渡してきたら、**必ず以下の順番**で進める。

### Step 1: ネット調査（必須）

以下のキーワードでWebSearchを実行する。

```
[店名] [エリア] スープ 麺 特徴
[店名] [エリア] レビュー 感想
```

収集する情報:
- スープのベース素材・味の構造（何種類の食材、どんな味噌など）
- 麺の種類・食感（ちぢれ/平打ち/中太、製麺所名など）
- トッピングの内容・特徴
- 卓上調味料（にんにく・ゆず七味・青のりなど）
- 店の雰囲気・席構成

> 写真で確認できる内容と照合し、矛盾する情報は使わない。

### Step 2: 豆知識のファクトチェック（必須）

記事末尾の豆知識（1記事1個・2〜3行）は、必ずWebSearchで事実確認してから書く。
ラーメンの歴史・素材・地域文化など、読んで「へえ」となる内容を選ぶ。

### Step 3: 記事を書く

調査結果を「材料」として使い、**ぽんの視点で**書く。

---

## ぽんのキャラクター

- 東京在住OL、20代前半、地方出身（非公表）
- **天然でお転婆**な性格
- ラーメン好きだが専門家ではない
- 学生時代は二郎系→最近はちゃん系などあっさり系に移行

### 文章スタイル

- 友達に話してるテンション
- 「たぶん」「なんか」「やばい」「もしかして」が自然に混ざる
- 食レポより日記に近い
- **グルメライター的表現はNG**（「じわっとうまみが広がって体の芯に届く」など）
- 調査で得た情報は「後から調べたら〜らしい」「知ってから飲むと〜」という形で自然に織り込む
- 単なる事実の羅列にならないよう、感情の流れと場面の情景を丁寧に描く

---

## フロントマター必須項目

```yaml
---
title: "..."
description: "..."
pubDate: YYYY-MM-DD
emoji: "🍜"
image: "/images/[slug].jpg"
tags: ["エリア名", "その他タグ"]
lat: 35.XXXX        # Googleマップで確認。マップピンの自動追加に使用
lng: 139.XXXX
shopName: "店舗名"  # マップのツールチップに表示される名前
---
```

`lat`/`lng` は必ず入れる。これがないとマップに自動追加されない。
値は `maps.google.com` で住所を検索し、URLのパラメータから取得する。

---

## 記事構成テンプレート

```
[リード文：入店までの経緯・その日の文脈]

[外観・店内の写真 optional]

## [見出し：食べた内容やポイント]

[料理写真]

[食べた感想。調査情報を自然に織り込む]

## [見出し：もう一つのポイントや気づき]

[追加の薬味・卓上・変化球など]

---

> [豆知識：1個・2〜3行・ファクトチェック済み]

---

## 店舗情報

| | |
|---|---|
| **店名** | 〇〇 |
| **住所** | 東京都〇〇 |

[Googleマップで見る →](https://maps.google.com/?q=...)

---

ぽん / 店名（エリア）食べたもの
```

---

## 画像の扱い

- ユーザーが提供した `.heic` は `sips` でJPEGに変換してから `public/images/` に配置
- ファイル名: `[slug]-01.jpg`, `[slug]-sign.jpg` など用途がわかる名前
- `slug` = 記事のファイル名（拡張子なし）

---

## デプロイ

```bash
git add [files]
git commit -m "add: 店名 記事"
git push
# Cloudflare Pagesが自動ビルド・公開
```

---

## 記事ルール（その他）

- 食べログ等のスコア記載なし
- 著名でないビル名は省く
- **写真で確認できない描写は入れない**（調査情報も写真と矛盾する場合は除外）
- 末尾署名: `ぽん / 店名（エリア）食べたもの`
- 同じ店への再訪は別記事でOK（スラグにバージョンや特徴を入れて区別）

---

## 四コマ漫画の投稿フロー

1. `python scripts/4koma_generate_prompt.py` でエピソードを考案（`scripts/4koma_staging/current_episode.json` に保存される）
2. 出力される **`combined_prompt`（1本の統合プロンプト）だけ** をChatGPTに1回貼り付けて、4コマがまとまった画像を1枚生成する
   - **バラバラに4回（パネルごと）生成させない。必ず1枚の統合画像。** ユーザーから繰り返し指摘された点なので厳守。
3. 生成された画像を `scripts/4koma_staging/combined.png` として保存
4. `python scripts/4koma_publish.py` を実行 → `public/images/` へ配置・ブログ記事生成・`git push`まで自動

`4koma_publish.py`は`combined.png`を優先的に探す。見つからない場合のみ旧フロー（`panel1〜4.png`を`compose_4koma.py`で結合）にフォールバックするが、通常は使わない。
